Read AGENTS.md first and follow it strictly.

Use the installed Vision Agents skill and Stream skills to connect the Audio Lesson screen to the Vision Agent so the AI teacher joins the same Stream call as the user. 

Add Expo API routes to start and stop the agent that proxy to the Vision Agent server. Require an authenticated user in the route handlers, validate the lesson ID against an allowlist, and accept only the selected language code from the client. Load the selected lesson server-side, derive the lesson goals, vocabulary, phrases, and AI teacher prompt from that server-side lesson, then include those derived values in the Stream call's custom data before creating the call. Do not trust client-provided lesson content, and do not treat custom data as authorization. Update the Python agent to actually consume those fields.

Make sure the agent config starts with the send-audio capability by default and grants go-live only when the route explicitly invokes goLive; remove any admin role or equivalent excessive permission. Generate the agent token server-side with the required capabilities, and do not expose any Stream secret or admin credential in the mobile app. Associate cleanup with a unique call or session ID so agent cleanup is idempotent and race-safe. On both the end-call path and the screen-unmount path, cancel or abort any pending start request and then clean up only the matching session ID. If a late start response arrives after a newer session has been started or stopped, immediately stop the returned session instead of leaving it running.

Do not expose any secrets in the mobile app. Keep the existing Stream audio flow intact. Show the agent connection status with idle, connecting, connected, and failed states.